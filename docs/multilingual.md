# Мультиязычность в WordPress

Этот документ описывает актуальные решения для реализации мультиязычности на сайте ГоЦИСС.

## Обзор решений (2024-2025)

### 1. Polylang (Рекомендуется)

**Статус:** Активно поддерживается, регулярные обновления

**Преимущества:**
- ✅ Бесплатная базовая версия
- ✅ Легковесный, не замедляет сайт
- ✅ Простой интерфейс
- ✅ Хорошая интеграция с ACF
- ✅ Поддержка кастомных типов записей
- ✅ SEO-friendly (отдельные URL для языков)
- ✅ Поддержка RTL языков

**Недостатки:**
- ❌ Нет автоматического перевода (только ручной)
- ❌ Ограниченный функционал в бесплатной версии
- ❌ Нет синхронизации контента между языками

**Цена:**
- Базовая версия: **Бесплатно**
- Polylang Pro: от €99/год (автоматический перевод, синхронизация)

**Подходит для:** Проектов с ограниченным бюджетом, где переводы делаются вручную

**Официальный сайт:** https://polylang.pro/

---

### 2. WPML (WordPress Multilingual)

**Статус:** Лидер рынка, активно развивается

**Преимущества:**
- ✅ Максимальный функционал
- ✅ Автоматический машинный перевод (DeepL, Google Translate)
- ✅ Синхронизация контента между языками
- ✅ Отличная интеграция с WooCommerce
- ✅ Поддержка переводов тем и плагинов
- ✅ Advanced Translation Editor
- ✅ Круглосуточная поддержка

**Недостатки:**
- ❌ Дорогой (от €99/год)
- ❌ Может замедлять сайт на больших проектах
- ❌ Сложнее в настройке
- ❌ Создает отдельные записи для каждого языка (увеличивает БД)

**Цена:**
- Multilingual CMS: **€99/год** (до 3 языков)
- Multilingual Agency: **€199/год** (неограниченно языков)

**Подходит для:** Коммерческих проектов, где нужен автоматический перевод и синхронизация

**Официальный сайт:** https://wpml.org/

---

### 3. TranslatePress

**Статус:** Молодой, но активно развивающийся плагин

**Преимущества:**
- ✅ Визуальный редактор переводов
- ✅ Переводит прямо на фронтенде
- ✅ Автоматический перевод (Google Translate, DeepL)
- ✅ SEO-friendly
- ✅ Поддержка WooCommerce

**Недостатки:**
- ❌ Меньше функций чем WPML
- ❌ Бесплатная версия очень ограничена
- ❌ Может быть медленнее на больших сайтах

**Цена:**
- Personal: **€79/год**
- Business: **€149/год**

**Подходит для:** Проектов, где важна простота перевода через визуальный редактор

**Официальный сайт:** https://translatepress.com/

---

### 4. Weglot

**Статус:** SaaS решение, облачный сервис

**Преимущества:**
- ✅ Очень простой в использовании
- ✅ Автоматический перевод всего сайта
- ✅ Не нагружает сервер (облачный)
- ✅ SEO-friendly

**Недостатки:**
- ❌ Дорогой (от $15/месяц)
- ❌ Зависимость от внешнего сервиса
- ❌ Ограниченный контроль над переводами

**Цена:**
- Starter: **$15/месяц** (1 язык, до 10k слов)
- Business: **$29/месяц** (3 языка, до 50k слов)

**Подходит для:** Быстрого запуска мультиязычного сайта без технических знаний

**Официальный сайт:** https://weglot.com/

---

## Рекомендация для проекта ГоЦИСС

### Вариант 1: Polylang (Бесплатный)

**Когда выбирать:**
- Ограниченный бюджет
- Переводы делаются вручную
- Нужна простота и легкость

**Установка:**
1. Установи плагин через админку WordPress
2. Активируй плагин
3. Настрой языки в **Языки → Языки**
4. Добавь русский и английский (или другие нужные языки)
5. Настрой переключатель языков в виджетах или меню

**Интеграция с темой:**
- Polylang автоматически работает с ACF полями
- Нужно будет переводить каждое ACF поле вручную для каждой языковой версии
- Используй функции Polylang для вывода переключателя языков

### Вариант 2: WPML (Коммерческий)

**Когда выбирать:**
- Нужен автоматический перевод
- Важна синхронизация контента
- Готовы платить за функционал

**Установка:**
1. Купи лицензию на wpml.org
2. Скачай плагин
3. Установи через админку WordPress
4. Активируй лицензию
5. Настрой языки и переводы

**Интеграция с темой:**
- WPML имеет специальные функции для ACF
- Можно настроить автоматический перевод ACF полей
- Более сложная настройка, но больше возможностей

## Реализация в теме

### Добавление переключателя языков

В `header.php` добавь переключатель языков:

```php
<?php
// Для Polylang
if (function_exists('pll_the_languages')) {
    pll_the_languages(array('show_flags' => 1, 'show_names' => 1));
}

// Для WPML
if (function_exists('icl_get_languages')) {
    $languages = icl_get_languages('skip_missing=0');
    foreach ($languages as $lang) {
        echo '<a href="' . esc_url($lang['url']) . '">' . esc_html($lang['native_name']) . '</a>';
    }
}
?>
```

### Работа с переводами в коде

Используй функции WordPress для перевода строк:

```php
// Вместо хардкода
echo 'Привет';

// Используй
echo __('Привет', 'gociss');
```

Затем создай файлы переводов в `languages/`:
- `gociss-ru_RU.po` - русский
- `gociss-en_US.po` - английский

### Работа с ACF полями

Polylang и WPML автоматически создают отдельные версии ACF полей для каждого языка. При редактировании страницы ты увидишь поля для текущего языка.

## Настройка SEO для мультиязычности

### Polylang + Yoast SEO

1. Установи оба плагина
2. Polylang автоматически интегрируется с Yoast SEO
3. Настрой hreflang теги (автоматически)
4. Для каждого языка настрой отдельные SEO параметры

### WPML + Yoast SEO

1. Установи WPML и Yoast SEO
2. WPML автоматически создаст отдельные SEO настройки для каждого языка
3. Настрой hreflang через WPML

## Структура URL

### Варианты структуры:

1. **Поддомены:** `ru.gociss.ru`, `en.gociss.ru`
2. **Префиксы:** `gociss.ru/ru/`, `gociss.ru/en/`
3. **Домены:** `gociss.ru`, `gociss.com`

**Рекомендация:** Используй префиксы (`/ru/`, `/en/`) - это проще в настройке и лучше для SEO.

---

## Мультирегиональность: поддомены vs директории

### Вопрос от SEO-компании

**Ситуация:** SEO-компания предлагает использовать одновременно:
- Поддомены: `spb.gociss.ru`, `msk.gociss.ru`
- Директории: `gociss.ru/spb/`, `gociss.ru/msk/`

**Аргументация:** Яндекс "любит" поддомены, Google "любит" директории.

### ⚠️ Критический анализ

#### Технически возможно, но НЕ рекомендуется

**Проблемы такого подхода:**

1. **Дублирование контента**
   - Один и тот же контент будет доступен по двум URL
   - Поисковики могут считать это дублированием
   - Риск попадания под фильтры за дубли

2. **Размытие SEO-сигналов**
   - Ссылочная масса делится между двумя URL
   - Социальные сигналы (лайки, шары) раздваиваются
   - Сложнее накапливать авторитет страницы

3. **Проблемы с индексацией**
   - Поисковики могут индексировать оба варианта
   - Нужны сложные настройки canonical и hreflang
   - Риск конфликтов в индексации

4. **Сложность поддержки**
   - Два источника правды для одного контента
   - Сложнее отслеживать аналитику
   - Больше точек отказа

5. **Пользовательский опыт**
   - Пользователи могут попадать на разные версии
   - Проблемы с сохранением сессий между поддоменами
   - Путаница в навигации

### ✅ Правильное решение

**Выбрать ОДИН вариант:**

#### Вариант 1: Только поддомены (для Яндекса)

```
spb.gociss.ru
msk.gociss.ru
ekb.gociss.ru
```

**Плюсы:**
- ✅ Яндекс лучше индексирует поддомены
- ✅ Четкое разделение регионов
- ✅ Можно использовать разные SSL сертификаты

**Минусы:**
- ❌ Google может хуже индексировать
- ❌ Сложнее настройка (нужны DNS записи)
- ❌ Дополнительные расходы на SSL (если не wildcard)

**Когда использовать:** Если основная аудитория в России и Яндекс - приоритет.

#### Вариант 2: Только директории (для Google)

```
gociss.ru/spb/
gociss.ru/msk/
gociss.ru/ekb/
```

**Плюсы:**
- ✅ Google лучше индексирует директории
- ✅ Проще настройка (не нужны DNS записи)
- ✅ Один SSL сертификат
- ✅ Проще управление контентом

**Минусы:**
- ❌ Яндекс может хуже индексировать
- ❌ Менее четкое разделение регионов

**Когда использовать:** Если нужна международная аудитория или Google - приоритет.

### 🔧 Если всё-таки нужно оба варианта

**⚠️ ВАЖНО: Твои опасения абсолютно обоснованы!**

**Проблема с дублированием контента:**
- ✅ Да, это **реальная проблема**
- ✅ Google и Яндекс могут считать это дублированием
- ✅ Страницы будут **конкурировать друг с другом** в поисковой выдаче
- ✅ Риск попадания под **фильтры за дубли**
- ✅ Размытие ссылочной массы между двумя URL

**Но технически возможно реализовать через Polylang:**

#### Техническая реализация через Polylang

Polylang поддерживает работу с поддоменами через фильтр `pll_link_type`. Можно настроить так, чтобы:

1. **Для одних регионов использовались поддомены:**
   - `spb.gociss.ru`
   - `msk.gociss.ru`

2. **Для других регионов использовались директории:**
   - `gociss.ru/ekb/`
   - `gociss.ru/nsk/`

**Но это требует сложной настройки:**

1. **Настройка Polylang для поддоменов:**

В `functions.php` или отдельном файле:

```php
<?php
// Определяем, какие регионы используют поддомены
function gociss_polylang_link_type($type) {
    // Регионы с поддоменами
    $subdomain_regions = array('spb', 'msk');
    
    // Получаем текущий язык/регион
    $current_lang = pll_current_language();
    
    if (in_array($current_lang, $subdomain_regions)) {
        return 'subdomain'; // Используем поддомены
    }
    
    return 'directory'; // Используем директории
}
add_filter('pll_link_type', 'gociss_polylang_link_type');

// Настройка доменов для каждого региона
function gociss_polylang_domains($domains) {
    return array(
        'spb' => 'spb.gociss.ru',
        'msk' => 'msk.gociss.ru',
        'ekb' => 'gociss.ru', // Для директорий основной домен
        'nsk' => 'gociss.ru',
    );
}
add_filter('pll_domains', 'gociss_polylang_domains');
?>
```

2. **Настройка сервера (nginx):**

```nginx
# Поддомены
server {
    server_name spb.gociss.ru msk.gociss.ru;
    root /path/to/wordpress;
    # ... остальная конфигурация
}

# Основной домен с директориями
server {
    server_name gociss.ru;
    root /path/to/wordpress;
    # ... остальная конфигурация
}
```

3. **Правильная настройка canonical (КРИТИЧНО!):**

В `header.php` или через фильтр:

```php
<?php
function gociss_canonical_url() {
    $current_lang = pll_current_language();
    $subdomain_regions = array('spb', 'msk');
    
    // Определяем канонический URL
    if (in_array($current_lang, $subdomain_regions)) {
        // Для поддоменов - canonical на директорию
        $canonical = 'https://gociss.ru/' . $current_lang . '/';
    } else {
        // Для директорий - canonical на себя
        $canonical = get_permalink();
    }
    
    echo '<link rel="canonical" href="' . esc_url($canonical) . '" />';
}
add_action('wp_head', 'gociss_canonical_url', 1);
?>
```

4. **Настройка hreflang:**

Polylang автоматически генерирует hreflang, но нужно убедиться, что указаны оба варианта:

```php
<?php
function gociss_hreflang_tags() {
    $languages = pll_the_languages(array('raw' => 1));
    
    foreach ($languages as $lang) {
        // Основной URL (директория)
        $url = pll_home_url($lang['slug']);
        echo '<link rel="alternate" hreflang="' . esc_attr($lang['slug']) . '" href="' . esc_url($url) . '" />';
        
        // Если регион использует поддомен, добавляем и его
        $subdomain_regions = array('spb', 'msk');
        if (in_array($lang['slug'], $subdomain_regions)) {
            $subdomain_url = 'https://' . $lang['slug'] . '.gociss.ru';
            echo '<link rel="alternate" hreflang="' . esc_attr($lang['slug']) . '" href="' . esc_url($subdomain_url) . '" />';
        }
    }
}
add_action('wp_head', 'gociss_hreflang_tags', 2);
?>
```

5. **Robots.txt для поддоменов:**

На каждом поддомене создать `robots.txt`:

```
User-agent: *
Disallow: /

# Или разрешить индексацию, но указать canonical
```

#### ⚠️ Критические моменты:

1. **Canonical обязателен!**
   - На поддомене canonical должен указывать на директорию
   - Иначе поисковики увидят дубли

2. **Hreflang должен указывать на оба варианта**
   - Но canonical должен быть один (директория)

3. **Риски остаются:**
   - Поисковики могут индексировать оба варианта
   - Даже с canonical есть риск проблем
   - Сложнее отслеживать аналитику

4. **Рекомендация:**
   - Если всё-таки делать оба варианта, лучше сделать **301 редирект** с поддоменов на директории
   - Это безопаснее для SEO

### 📊 Рекомендация для ГоЦИСС

**Выбрать ОДИН вариант:**

1. **Если приоритет Яндекс (российская аудитория):**
   - Использовать **только поддомены**
   - `spb.gociss.ru`, `msk.gociss.ru`, `ekb.gociss.ru`
   - Google тоже будет индексировать, просто чуть хуже

2. **Если приоритет Google (международная аудитория):**
   - Использовать **только директории**
   - `gociss.ru/spb/`, `gociss.ru/msk/`, `gociss.ru/ekb/`
   - Яндекс тоже будет индексировать, просто чуть хуже

3. **Компромиссное решение:**
   - Использовать директории (`/spb/`, `/msk/`)
   - Правильно настроить hreflang и canonical
   - Оба поисковика будут индексировать нормально

### 💡 Ответ для заказчика

**Можно ли добавлять страницы с услугами в обоих вариантах одновременно?**

**Технически:** Да, можно.

**Но НЕ рекомендуется** по причинам:
- Риск дублирования контента
- Размытие SEO-сигналов
- Сложность поддержки
- Проблемы с аналитикой

**Рекомендация:**
1. Выбрать **один вариант** (рекомендую директории `/spb/`, `/msk/`)
2. Правильно настроить SEO (canonical, hreflang)
3. Оба поисковика будут индексировать нормально
4. Проще поддерживать и развивать

**Если SEO-компания настаивает на обоих вариантах:**
- Попроси их обосновать технически
- Уточни, как они планируют решать проблему дублей
- Попроси примеры успешных проектов с таким подходом
- Предложи начать с одного варианта, потом добавить второй при необходимости

### 📚 Полезные ссылки

- [Google: Поддомены vs подпапки](https://developers.google.com/search/docs/specialty/international/managing-multi-regional-sites)
- [Яндекс: Мультирегиональные сайты](https://yandex.ru/support/webmaster/multiregional-site/multiregional-site.html)
- [Moz: Subdomains vs Subdirectories](https://moz.com/learn/seo/domain)

## Чеклист внедрения

- [ ] Выбрать плагин мультиязычности
- [ ] Установить и активировать плагин
- [ ] Настроить языки сайта
- [ ] Добавить переключатель языков в шапку
- [ ] Перевести все ACF поля для каждого языка
- [ ] Перевести меню для каждого языка
- [ ] Настроить SEO для каждого языка
- [ ] Протестировать переключение языков
- [ ] Проверить работу форм на всех языках
- [ ] Настроить редиректы для правильного языка

## Полезные ссылки

- [Polylang документация](https://polylang.pro/doc/)
- [WPML документация](https://wpml.org/documentation/)
- [WordPress i18n руководство](https://developer.wordpress.org/advanced-administration/features/internationalization/)

## Вывод

**Для проекта ГоЦИСС рекомендую:**

1. **Если бюджет ограничен:** Polylang (бесплатный) - простое и легкое решение
2. **Если нужен автоматический перевод:** WPML - максимальный функционал
3. **Если важна простота:** TranslatePress - визуальный редактор переводов

Учитывая, что в ТЗ упоминается Polylang, рекомендую начать с него. Если понадобятся дополнительные функции (автоперевод, синхронизация), можно будет перейти на WPML.

